# Reade 稳定性与可维护性改造：Agent 执行计划

> 文档版本：1.0 · 编制日期：2026-09-05  
> 目标仓库：`RsLuna7/reade`  
> 已核对的远端基线：`94ef1dd94c0576f5916ac5935458876d4feca249`  
> 默认执行范围：Windows 桌面版；Web 保持现有构建与阅读兼容。  
> 本文是待执行规格，不是完成报告。审查发现主要来自静态代码；没有在本轮执行项目测试、迁移数据库或修改仓库。

## 0. 给执行 Agent 的任务

在保留 Reade 现有阅读体验、离线边界和历史数据的前提下，完成一次有回归证据的稳定性、性能和可维护性改造。

**执行顺序：建立基线 → 建立上下文契约 → 修复数据与并发问题 → 修复 PDF/EPUB 生命周期 → 实测优化 → 分批拆分代码 → 最终验收。**

不要重写项目，不要把审查意见无条件当作已确认缺陷，也不要只输出另一份计划。进入实现后，每个任务执行“读实现 → 复现或测量 → 最小修改 → 定向验证 → 记录”。普通技术决策按本文默认值推进；涉及真实用户数据冲突、发布或权限扩大时保持安全边界，记录阻塞，继续可独立完成的任务。

### 0.1 交付范围

- 默认任务：`D00–D16`，其中性能任务允许以“测量证明无需修改”结项，必须附证据。
- 可选任务：`W01–W04`。**默认不执行**；仓库 `AGENTS.md` 已明确 Web 功能封存。只有获得单独的 Web 修复授权，或本轮共享代码改动引入 Web 构建/安全回归，才进入相应最小修复。[R01]
- 不包含：编辑器、AI 问答、OCR、账号、云同步、遥测、自动更新、全新主题系统、大规模产品改版。
- 不包含：自动提交、创建分支、推送、发布、修改远端分支保护、改写 `AGENTS.md`。这些不是本计划默认授予的操作。
- 允许新增：必要的测试、错误与会话基础设施、本地诊断/恢复入口、与改造直接有关的小型开发依赖。

### 0.2 必须遵守的边界

1. 先读取仓库根目录 `AGENTS.md`、相关源码及现有测试；已有事实以当前源码为准。实际 HEAD 不同则记录差异，**不 checkout/reset 到审查提交**。
2. 保留用户未提交改动。禁止 `git reset --hard`、`git clean -fd`、全仓格式化、覆盖无关文件和静默删除文件。
3. 使用 pnpm；不手改锁文件。新增依赖必须解释用途，并由包管理器更新锁文件。不要顺手升级 React、Tauri、PDF.js 或全部依赖。
4. 保持三栏布局、快捷键、现有主题、已有导入导出格式、标注与回顾语义。结构重构不得夹带产品行为变化。
5. 保持 raw HTML/MDX 代码禁用、外链确认、远程图片默认关闭、SVG/Mermaid 清洗、路径 canonicalize 与书库内边界验证。不得为解决功能问题放宽 CSP 或文件访问权限。
6. 前端系统调用继续通过 `backend.ts → tauriBackend.ts → Rust commands`；不得在组件内新增绕过 facade 的任意文件访问。[R01]
7. 数据迁移、清空、故障注入和恢复测试只使用临时目录及合成/复制数据。**不得对实际用户数据库试跑迁移或清理**。
8. 不把“测试绿了”“生成了安装包”表述成“Windows 真机验收通过”。未运行的项目必须标为 `NOT_RUN`。
9. 临时中断时交付检查点，不把部分工作写成全部完成；下次从状态文件恢复，不重新规划全项目。

### 0.3 发现分级规则

每个审查项在 `D00` 中归类：

| 类别 | 定义 | 处理 |
|---|---|---|
| `CONFIRMED` | 已有可复现失败用例或确定性的行为证据 | 先保留失败测试，再修复 |
| `RISK` | 代码显示可能触发，但尚未复现 | 增加最小测试/故障注入；不得虚构发生概率 |
| `IMPROVEMENT` | 可维护性、性能或体验建议 | 给出基线与目标后实施 |
| `RESOLVED` | 当前 HEAD 已修复 | 补证据和缺失测试，不重复改写 |
| `NOT_APPLICABLE` | 当前支持范围或实测路径不成立 | 解释原因；保留审计记录 |
| `DEFERRED` | 例如封存 Web 或收益不足的大改 | 明确后续触发条件 |

特别说明：Windows 上 `app_cache_dir` 通常解析到 LocalAppData 下，不等于系统临时目录；“当前存放路径”与“已经被系统自动删除”不是同一个结论。仍应区分持久数据与可再生缓存，但迁移前必须核对真实路径、旧版本行为与清理范围。[O01]

## 1. 执行状态、交付文件与任务单

在仓库中新增以下工作文档；已经存在等价文件时复用，避免重复体系：

```text
docs/hardening/
  BASELINE.md             # 实际 HEAD、环境、原有改动、测试与性能基线
  FINDINGS.md             # 30 项审查意见的确认/排除/延后情况
  CONTRACTS.md            # 书库/文档会话、错误、持久化与锁顺序契约
  TASKS.md                # D/W 任务状态与依赖
  VALIDATION.md           # 实际命令、退出码、用例、证据路径
  HANDOFF.md              # 当前检查点、下一项、未解决风险
  FINAL_REPORT.md         # 最终差异、风险与验收结论
```

截图、日志、性能样本和合成大文件放 `output/hardening/`，按现有忽略策略管理，不自动提交。不要把真实文档、绝对私有路径、笔记正文或凭据放进报告。

每个任务至少记录：

```markdown
### Dxx — 任务名
状态：TODO | IN_PROGRESS | DONE | BLOCKED | NOT_APPLICABLE | DEFERRED
依赖：
当前代码证据：文件、函数、当前 HEAD 行范围
变更前复现/测量：
修改文件：
实现决策与兼容性：
实际验证命令、退出码及证据：
未验证范围：
下一步：
```

`DONE` 要求该任务验收项全部通过。环境缺失、失败测试未解释、只写实现未验证，均不能标为 `DONE`。若无须修改，使用 `NOT_APPLICABLE` 或记录“已有实现满足要求”的明确证据，不造工作量。

## 2. 阶段与依赖

| 阶段 | 任务 | 阶段出口 |
|---|---|---|
| M0 基线与门禁 | D00、D01；D10 先采集基线 | 已知当前真实状态；新测试能够被执行 |
| M1 数据与上下文 | D02、D03、D04、D05 | 旧结果不串库；迁移可验证；统计归属与重试明确 |
| M2 多格式稳定性 | D06、D07、D08 | PDF 错误可终止；双栏 EPUB 不互相破坏；解析有资源限制 |
| M3 可测量性能 | D09、完成 D10、D11 | 扫描/渲染有前后对比；不存在功能回退 |
| M4 可维护性与诊断 | D12、D13、D14、D15 | 分模块可测试；本地错误可诊断；数据有恢复路径 |
| M5 最终验收 | D16 | 验证矩阵、差异、限制和交接完整 |

依赖细则：

```text
D00 → D01
D00 → D02 → D03
D00 → D04
D02 + D04 → D05
D02 → D06
D02 → D07 → D08（资源预检可提前独立实现）
D02 + D10基线 → D09
D06 + D07 + D08 + D10基线 → D11
D03 + D05 + D06 + D07 → D12
D04 + D08 + D09 → D13
D12 + D10基线 → D14
D04 + D05 + 错误契约 → D15
全部默认任务结项 → D16
```

单 Agent 按依赖顺序执行。多 Agent 只能在互不重叠文件集上并行；`App.tsx`、`backend.ts`、`tauriBackend.ts`、`library.rs`、`user_store.rs` 及锁文件必须有单一当前编辑者。模块拆分不能与同一模块的行为修复并行。

## 3. 跨任务共同契约

### 3.1 不混用三种“版本”

当前实现中存在前端请求世代与后端索引 generation。不要把它们机械合并为一个计数器：普通刷新不应无故终止仍合法的阅读会话。[R02][R03]

建议明确三层，具体命名可以沿用当前仓库：

- **书库身份 `libraryKey`**：由 Rust 校验并规范化的书库根路径所对应的持久身份。先沿用现有数据库 root key，不借本次改造重定义历史数据主键。
- **打开会话 `librarySessionId`**：由 Rust 为每次成功打开书库产生。A→B→A 必须是不同会话，用来拒绝过期 UI 结果和新发起的过期调用。
- **扫描修订 `scanRevision`**：普通刷新/索引变化使用；不等价于切库。
- **文档会话 `documentSessionId`**：绑定书库会话、相对路径和打开时文件版本，主栏/副栏分别拥有阅读生命周期。

文件版本至少含原有 size/mtime；必要时结合文件句柄/指纹。不能宣称 size/mtime 是完整内容一致性或安全身份保证。

建议的内部类型示意，**不是未经验证可直接复制的现成 API**：

```ts
type LibraryContext = Readonly<{
  libraryKey: string;
  librarySessionId: string;
}>;

type DocumentContext = Readonly<LibraryContext & {
  relativePath: string;
  documentSessionId: string;
}>;

type ReadeError = Readonly<{
  code: string;
  message: string;
  retryable: boolean;
  requestId?: string;
}>;
```

关键行为：

1. 异步动作发出时捕获上下文；成功、失败、清理、撤销栈和 loading 回写均检查上下文。
2. 仅前端忽略响应不够：Rust 不得在 await 后重新把旧请求解析到“当前书库”的同名文件。
3. 新发起的旧会话调用应拒绝。已校验并进入事务的旧写入允许完成到**原库**，但不得污染新界面；已经成功持久化的数据不能为“取消”而自动删除。
4. 事件必须携带原会话身份；旧 watcher/索引事件不得改写新库同名文档的状态。
5. 搜索和读取具有 latest-wins 语义；用户写入不应不加区分地 latest-wins 后静默丢失。
6. 暂时只迁移本计划涉及的 commands；同步所有调用点、测试 mock、订阅者和双栏调用。不得留下“桌面调用忘带 token 时默认接受”的隐式绕过。
7. 使用运行时 facade 隔离桌面契约，不为封存 Web 新造一套文件会话后台。

### 3.2 错误与取消

先为改造触及的路径建立最小错误集：`STALE_CONTEXT`、`SOURCE_CHANGED`、`SOURCE_UNAVAILABLE`、`IO_TIMEOUT`、`DB_BUSY`、`STORAGE_FULL`、`MIGRATION_FAILED`、`RECOVERY_REQUIRED`、`RESOURCE_LIMIT`。

取消和上下文过期是可预期终止，通常不弹出“严重错误”；真正读取失败和保存失败必须可见。存量字符串错误通过统一适配器转换，禁止一次性重写所有 command 的错误文案。

Rust 内部保留原因链；前端拿到稳定 code 与可操作提示。脱敏日志只含操作类型、格式、请求 ID、耗时和错误码，不默认记录文档正文、选区或完整文件路径。[O02]

### 3.3 持久化与回滚

- 用户数据库、统计数据库、偏好与位置不是可清理派生数据；缓存清理必须使用具体文件/表的允许列表，不能递归删除应用根目录。
- SQLite 备份使用在线备份 API 或经验证的 `VACUUM INTO` 等一致性方式；不能只复制正在使用的 `.sqlite3` 主文件而忽略 WAL。[O03][O04]
- 迁移成功后保留原始快照和迁移日志；中断后可识别并恢复，不重复搬运。
- 数据冲突不能按文件 mtime 擅自择新、覆盖或合并。
- 代码回滚不等于可以降级数据库。禁止恢复旧备份覆盖迁移后新产生的数据；不支持的降级必须明确拒绝并给出导出/恢复说明。
- 核心数据 schema 变更只走递增迁移；不因模块重构重置 schema。缓存纯新增表优先沿用当前可兼容策略，不无故触发全库重索引。[R01]

## 4. 具体任务

### D00 — 固定实际基线与验证范围

**目的**：区分真实缺陷、历史已修复项、环境问题和纯优化建议。

**读取**：`AGENTS.md`、`README.md`、`package.json`、`pnpm-lock.yaml`、`src-tauri/Cargo.toml`、`Cargo.lock`、`vite.config.ts`、`docs/bug-audit-2026-08-30.md`、现有 `docs/plan-*.md` 中与将改动功能对应的文件。

**执行**：

1. 记录 `git status --short`、`git rev-parse HEAD`、`git diff --stat`，区分原有改动。不得为形成干净基线丢弃用户改动。
2. 记录 Windows/架构、Node、pnpm、Rust、WebView2 和构建工具版本；检查实际锁定依赖，不按本文猜版本。
3. 跑第 5 节当前存在的验证命令，记录原有失败和构建警告。历史审计通过数字不等于当前测试结果。
4. 在临时目录建立测试书库 A、B：同路径不同内容的 MD/PDF/EPUB、不同图片、重复锚点及标注；另建用于迁移的合成数据库。
5. 对第 8 节 30 项建立状态映射，每项附当前源码符号及证据。特别核对标注目录、异步写回、PDF Range、统计切库、EPUB 单缓存、Web 封存范围。
6. 读取现有测试辅助工具；优先扩展，不为每项建一套重复 mock 框架。

**验收**：`BASELINE.md` 可复现环境；所有 30 项有初始分类；未执行测试与环境阻塞明确；未写用户数据、未改应用行为。

### D01 — 测试门禁与工具链约束

**修改位置**：`package.json`、`.github/workflows/`、必要的工具链配置；现有 `.github/workflows/deploy-pages.yml` 只做与验证门禁直接有关的最小修改。[R04]

**实现**：

1. 在 `package.json` 声明实际验证过的精确 pnpm `packageManager`，Node `engines` 与现有 Node 24 要求保持一致。Rust 固定已验证工具链，不直接升级到未知版本。
2. 新增 `verify.yml`：PR 与主分支变更运行前端测试、类型检查、桌面前端构建；Windows runner 运行 Rust test/fmt/clippy。优先使用 Windows 验证桌面，不以 Linux Web 构建替代。
3. Web 仅执行现有 `build:web` 兼容性检查，不新增 Web 产品验收目标。
4. 发布作业必须依赖同一待发布提交的验证结果。可以使用同一工作流的 `needs` 或显式可复用工作流；不要仅凭另一个工作流“可能已经成功”。保持现有发布触发条件，不在本轮执行发布。
5. CI 默认 `contents: read`；发布权限只放发布 job。不要对外部 PR 使用携带秘密的 `pull_request_target` 执行不可信代码。
6. 加入依赖审计入口：使用实际可用且版本固定的 pnpm/Rust 审计工具。先形成报告和有期限的例外清单，再制定阻断策略；不能为了过门禁关闭整类检测。
7. 必须新增浏览器/桌面 E2E 驱动时，仅加开发依赖，并提供准确 script；**不得调用当前不存在的 `pnpm lint` 或 `test:e2e`**。

**验收**：workflow 可解析；本地同命令可执行；失败测试会阻止后续发布任务；没有扩大线上权限、推送或发布。远端工作流未实际运行时，如实标记“配置就绪，远端未验证”。

### D02 — 书库、文档上下文与过期任务隔离

**修改位置**：`src-tauri/src/library.rs`、`lib.rs`、`src/lib/backend.ts`、`tauriBackend.ts`、`src/store/useReaderStore.ts`、事件订阅及其测试。

**实现**：

1. 按 §3.1 写 `CONTRACTS.md`，明确 librarySession 与 scanRevision；保留原有前端请求序号的有效逻辑，不直接删除历史跨库防护。[R02][R03]
2. 开库请求自身也要防乱序：A 开库慢、B 开库快时，较晚完成的 A 不得覆盖 B 的后端根目录。使用后端请求序号/提交校验，不只过滤前端 Promise。
3. 为文档打开、索引提交、PDF 阅读模式、预览/资源读取和相关事件接入上下文。提交索引结果前核对原始库、文档版本及适用的修订。
4. 扫描刷新不改变同库活动阅读身份；文件确实变化时只失效相关文档句柄。
5. 统一过期判断 helper 与错误适配；避免每个组件重新定义略有差异的条件。
6. 在 Rust 建立明确锁顺序。不要跨 `.await` 持有全局状态锁；不要因减少锁而把校验和实际读写重新分配到不同书库。

**先补测试**：

- `opening_a_then_b_cannot_commit_a_last`。
- A/B 同名 EPUB：A 解析迟到不更新 B 的标题、状态或资产。
- A→B→A 时第一次 A 的事件被丢弃。
- 普通刷新不取消仍有效的阅读会话；源文件变更使对应会话失效。
- 旧索引错误不出现在新书库；旧操作结束不清除新操作的 loading。

**验收**：前后端均有确定性竞态测试；使用 barrier/deferred promise，不依靠随机 sleep；所有桌面调用点同步；同库刷新原有能力不回退。

### D03 — 标注写入、重载与撤销的上下文一致性

**修改位置**：`src/lib/useDocumentAnnotations.ts` 及测试、`App.tsx` 的接线、对应 Rust 写命令；必要时提取独立的标注状态 reducer。

**代码线索**：当前 reload 有令牌，但多种 mutation 在 await 后直接使用当前 bundle/ref。需要验证的是完整写路径，不是重复给 reload 再加一个令牌。[R05]

**实现**：

1. hook 显式接收文档上下文，mutations 捕获不可变上下文；返回后同时保护 bundle、error、loading 与 undoStack。
2. 覆盖 `save`、`saveExcerpt`、`remove`、`clearAll`、`undo`、`updateColor`、`saveReflection`、回顾 enrollment；不要只修新建摘录。
3. 同文档写操作使用局部有序队列或带 revision 的更新策略；修改 note/color 时不得以过期整条对象覆盖另一个刚成功的字段。
4. reload 发起后若有新写入成功，旧 reload 不得倒退本地状态。以 revision/请求基线或写后重新查询保证一致性。
5. 清空与撤销绑定原文档；切换文档清理当前显示的撤销入口，迟到返回不得把旧栈重新塞入。
6. 已成功写入原库而 UI 已切走时，不回滚那条数据；在下次打开原文档时正常读取。失败必须有可重试反馈。
7. 关闭/卸载后不回写 UI，不把吞错当取消。

**先补测试**：

- A 新建摘录未完成→切 B→A 成功返回：B 列表与撤销栈不变；A 数据正确。
- A 清空未完成→切 B→清空返回：B 不被清空。
- A 保存感悟→同库同路径新文档版本→旧响应不得混入。
- reload 比 mutation 晚返回时，新摘录不消失。
- note 与 color 并发更新均保留；失败撤销仍可重试。
- 切回 A 后没有重复摘录或错误删改。

**验收**：以上情形有失败前/通过后证据；内存状态与 Rust 持久层均核对；既有标注导入、回顾和旧数据投影测试仍通过。

### D04 — 标注数据库位置与无损迁移

**修改位置**：`src-tauri/src/lib.rs`、`user_store.rs`；建议新增 `storage_paths.rs`、`storage_migration.rs`，不要混入大型数据模型重构。

**现状**：启动把 `UserState` 放入 cache directory，统计使用 data directory；原有 user store 含迁移、升级备份及从旧缓存救援的逻辑，必须复用。[R06][R07]

**默认落点**：保持统计位置不动，把用户数据库迁至 `app_data_dir/reade-user.sqlite3`；转换缓存仍保留原位。先验证该目标与实际平台路径；不得把仅改为 `app_local_data_dir` 当成一定完成物理隔离。[O01]

**迁移决策表**：

| 情况 | 行为 |
|---|---|
| 新旧均不存在 | 在新位置初始化 |
| 新旧规范化后同一文件 | 不迁移、不复制；继续使用现有文件 |
| 只有旧库 | 一致性备份到新目录临时文件→校验→发布新库→写迁移记录；保留旧库 |
| 只有新库 | 按现有迁移链正常打开 |
| 两者存在且迁移记录证明旧库未再变化 | 使用新库，不反复覆盖 |
| 两者都存在但无可信迁移记录，或旧版又产生了新写入 | 不静默合并/覆盖；保护两份，进入恢复提示 |
| 目标只读、空间不足、校验失败、迁移被打断 | 不破坏源库；记录可恢复状态，不创建空库冒充成功 |

**实现细节**：

1. 把持久路径与 `legacy_cache_path` 分开传给 `UserState`，保证从旧缓存中救援标注的链路先于可再生缓存重建。
2. 备份使用 SQLite 一致性机制；包含 WAL 中已提交数据。临时文件在目标目录，成功后再发布。[O03][O04]
3. 对备份执行 `PRAGMA integrity_check`，按业务表核对 schema、ID 集合、行数及必要逻辑摘要。单独比较文件大小不足以证明无损。
4. 迁移记录包含版本、源/目标身份、完成阶段、校验结果；不能仅用 mtime 判定哪个数据库更新。
5. 两个应用实例同时启动的迁移要互斥；失败后重启幂等。保留已有 schema 递增链，不为了换目录重建业务表。
6. 清缓存仅删除明确定义的派生缓存；用户库、统计库、备份和偏好不能被匹配。
7. 迁移本身不自动删除旧数据。后续清理需独立说明和用户操作。

**先补测试**：旧库独有；新库独有；两者冲突；同路径；含未 checkpoint WAL；目标不可写；中途失败；第二次启动；旧缓存救援；新 schema 被旧程序拒绝；清缓存前后用户记录一致。

**验收**：全部使用临时数据；失败不损坏源；成功后标注/感悟/合集/回顾/指纹及 tombstone 保留；正常启动路径不要求手工拷贝数据库。

### D05 — 阅读统计会话绑定、重试与关闭落盘

**修改位置**：`src/lib/readingTracker.ts`、相关 tests、`App.tsx`、`src-tauri/src/stats.rs`、facade；依赖 D02、D04。

**代码线索**：后端按保存时当前 root 归属；前端结束会话后清空 session，使其保存失败未必还有下一次 flush。[R08][R09]

**实现**：

1. 开始会话时让后端验证原文档上下文并绑定 `sessionId → libraryKey + relativePath`。后续保存按已绑定的会话处理，不再按保存瞬间的 current root 重新归属，也不接受未经校验的任意 root。
2. 会话在切库后仍可完成最后保存；新开 A/B 同路径时必须是不同会话。sessionId 不能跨库复用。
3. 保留固定会话 ID 幂等 upsert；增加 revision/单会话串行队列，防止 60 秒快照先返回、30 秒快照后到导致统计倒退。
4. 将已结束但未确认的快照留在待写队列中。建议退避 1/2/4/8/30 秒并设置内存上限；只合并同一会话的更新快照，不静默丢弃其他会话。
5. 正常关闭窗口时协调 flush 与关闭：有界等待，例如最多 3 秒；失败显示未保存状态或提供继续关闭选项。不要无限阻止关窗。
6. 跨进程重启的待写恢复必须使用已绑定会话的持久记录/可验证 outbox；不要把完整个人记录塞进新的不受管理 localStorage。
7. 明确承诺边界：进程被强杀前尚未持久化的最后一个采样区间可能丢失；只能承诺已经确认写入的数据，不能声称所有断电情形零丢失。
8. 保持焦点/可见性/空闲阈值和统计显示口径；schema 有变化时新增迁移，不清空历史统计。

**先补测试**：切库末次 flush；结束后失败再成功；同名文件跨库；重复提交幂等；乱序快照不倒退；旧会话不能写到新库；关窗等待成功/超时；重启恢复未确认快照；跨午夜归属不回退。

**验收**：检查数据库中的库归属、会话 ID、活跃秒数，而不是只检查 Promise resolve；重复重试不重复累计；未保存状态可见且可诊断。

### D06 — PDF Range 读取、错误传播与文件会话生命周期

**修改位置**：`src/components/PdfReader.tsx`、其 tests、`src-tauri/src/library.rs` 的 Range 命令、facade；可新增 `pdfTransport.ts`、`document_sessions.rs`。

**实现**：

1. Range transport 绑定 D02 的文档句柄。相对路径只用于打开时选择，后续 Range 不能随切库指向另一份同名 PDF。
2. 保留 Rust 单次最多 4 MiB 限制。PDF.js 若请求更大区间，按有界分段完整响应请求区间，或走其版本支持的明确定义策略；不能截短后当成完整响应。
3. 替换静默 catch：transport 把失败上报到阅读器；终止当前 loading task、释放资源、显示可重试状态。**先核对当前 PDF.js 公共 API，不得臆造 `onDataError` 等方法**。[R10]
4. 处理删除、替换、文件缩短、权限变化与已失效句柄；返回 `SOURCE_CHANGED` 等稳定错误，不返回空数组伪装读取成功。
5. 增加并发上限和待处理队列；建议初始并发 4，超时 15 秒，作为可测的内部常量。正常关闭取消不触发用户错误提示。
6. 资源销毁幂等：取消页 render/text task→停止新 Range→销毁加载任务/文档→释放后端句柄；晚到回调不访问已销毁 proxy。
7. 同文件双栏按引用计数管理；关闭一栏不破坏另一栏。兼顾封面缩略图对 PDF 的独立读取。

**先补测试**：第二段读取失败；超时；中途切库同名文件；文件更改；请求超 4 MiB；abort 后回调；主副栏关闭其一；失败点击重试；反复打开/关闭 20 次句柄不单调增长。

**验收**：失败能从加载态退出；没有无限等待、空响应假成功或串文件字节；分段上限与路径安全测试继续通过；真机验证原版式、阅读模式、缩放、双页、标注和页码恢复。

### D07 — EPUB 多会话资产与分栏锚点隔离

**修改位置**：`src-tauri/src/library.rs` 的 `open_epub/read_epub_asset`、`src/components/EpubReader.tsx`、`SecondaryPane.tsx`、相关 facade/tests。

**实现**：

1. 用按文档会话管理的资产集合替代单一 `open_epub`；记录文档版本、资产字节占用、活跃引用及最近使用。[R03][R11]
2. 打开 Markdown/PDF 不清空仍被其他阅读面使用的 EPUB；同 EPUB 双开可共享不可变资产，但阅读生命周期独立。
3. 建立有界缓存：建议初始总预算 128 MiB；优先淘汰未引用条目。单书或活跃集合超预算时明确降级/按需重读，不能无限保留，也不能静默逐出活跃资源。最终预算由 D10 样本校准。
4. asset request 使用文档句柄、assetId，后端仍检查 MIME/安全类型；主栏和副栏都显式释放引用。
5. DOM ID 加阅读面命名空间；书内原始锚点作为逻辑标识，转换到当前实例的 DOM ID。不要只对书名哈希，同书双开仍会冲突。
6. 跳转限定在当前 `rootRef` 内，移除本功能对全局 `document.getElementById` 的依赖；目录、脚注、书签、标注定位同步更新。[R11]
7. 保持已有不支持附件/危险链接的策略，不借修锚点扩大书内任意资源访问。

**先补测试**：主 A EPUB+副 B EPUB；主 EPUB+副 Markdown/PDF；同 EPUB 双栏脚注/目录各跳本栏；两本书都含 `id="chapter1"`；关闭一栏另一栏图片保留；资产更改不返回旧版本；缓存淘汰不影响仍活跃实例。

**验收**：图片、脚注和目录无跨栏影响；引用释放可观察；没有放宽格式安全边界。

### D08 — EPUB 容器与解析资源限制

**修改位置**：`src-tauri/src/documents.rs`、解析辅助模块、`Cargo.toml` 仅在必要时调整；测试使用合成小 ZIP。

**复核要求**：先检查锁定版本 `anydoc`/ZIP 解析库是否已经提供解压预算，不把包装层没写限制直接宣称为已证实漏洞。[R12]

**实现**：

1. 在容器预检中核对条目数量、单条目未压缩大小、累计解压量；对实际读取再设上限，不能只信 ZIP 元数据。
2. 建议初始预算：20,000 条目、单条目 32 MiB、累计 256 MiB、单 XML 4 MiB、转换后节点 200,000、递归深度 128。**这些是实施候选值，不是现有能力或实测结论**；用正常样本校准后写入具名常量，不放宽原有 128 MiB EPUB 文件上限。
3. XML 读取使用有界 reader。若当前正则提取无法正确处理合法命名空间/属性，改为已有或最小 XML parser；保持外部实体与外部资源不解析。
4. 大小/深度限制要覆盖后续 anydoc 解析与 DTO 转换；只预检一次但解析器自行无限解压不算完成。
5. 对严重畸形文件明确返回 `RESOURCE_LIMIT`，正常阅读功能保留；压缩比可作为辅助信号，不能单凭高压缩比误拒正常书籍。
6. 取消/超时需说明真实作用：取消 async future 并不会强行停止已经运行的阻塞解析。优先使用可协作的配额/中止；若第三方调用无法被限制，评估隔离 worker 进程并验证终止，未完成前明确记录残余 CPU 风险，不能把 UI 超时当解析已停止。

**先补测试**：超条目数；超总量；单 XML 超限；伪造大小/实际读取超限；深层节点；损坏条目；DRM/固定版式原有行为；正常图文 EPUB；取消后队列仍可处理下一本。

**验收**：限制贯穿读取与转换；恶意样本不会在测试中真实分配巨量内存；合法样本不被无故拒绝；残余不可中止路径明确列出。

### D09 — 扫描、索引和全局锁竞争

**修改位置**：`src-tauri/src/library.rs` 及其测试；必要时新增 `scan.rs`、`index_jobs.rs`，本任务只做解决锁竞争所需的提取。

**实现**：

1. 将 `open_library` 的磁盘扫描与其他阻塞 I/O 移入统一 blocking 调度；`async fn` 本身不意味着内部阻塞操作消失。[R03][O02]
2. 拆为“捕获已验证上下文→锁外扫描/读取→短事务更新缓存→校验后发布快照”。任何跨阶段对象都绑定原库和 scanRevision。
3. 数据库连接操作用明确的短事务/独立执行器；不要为减少锁直接无限创建连接或为每个文件 spawn 无界任务。
4. 合并 watcher 突发刷新；书库切换丢弃旧 watcher；防止一次大文件复制导致重复全库扫描。
5. 后台索引与主动打开任务设置有限队列，优先处理用户正在打开的文档；不改变全文搜索结果语义。
6. 保留现有缓存增量失效、FTS5、扫描排除规则、路径验证和性能用例，不能退化为每次全库重建。

**测试**：可控慢扫描时其他允许的命令可完成；A 扫描迟到不覆盖 B；watcher 高频事件可合并；扫描中删除文件可恢复；中文/空格/长路径；锁顺序无死锁；现有合成性能测试继续通过。

**验收**：D10 同机器前后数据证明锁等待/前台延迟改善或不回退；没有把所有读取串成新的单一长队列。

### D10 — 性能基线、预算与可重复测量

**修改位置**：`scripts/` 新建合成数据/性能脚本、可选 `tests/performance/`；只在开发/测试启用观测，不加入远程遥测。

**样本**：

| 样本 | 建议构成 | 用途 |
|---|---|---|
| 小书库 | 30 篇 MD，含中英文、公式、代码和图片 | 启动与基本功能 |
| 大书库 | 5,000 篇合成 MD，总计约 100 MiB | 扫描、搜索、文件树 |
| 长 EPUB | 200 章、300 张小图，总量受控 | 章节挂载与资产内存 |
| 多页 PDF | 500 页可提取文本、另一份扫描样本 | Range、页渲染与内存 |
| 高标注密度 | 单文档 1,000 条、全库 10,000 条 | 绘制、筛选、写回 |
| 长代码文档 | 多个代码块及单个大代码块 | Shiki 主线程耗时 |

大型样本运行时生成或本地提供，不提交有版权的真实书籍，不把性能样本塞进每次普通单测。

**测量要求**：

1. 固定同一机器、build 模式、窗口尺寸、缩放和样本；区分冷/热缓存、首屏/索引完成。
2. 至少记录：应用壳就绪、首次正文可读、搜索延迟、扫描与锁等待、主线程长任务、峰值工作集、活跃资源/句柄数、初始 JS gzip 及重型 chunk。
3. 热路径至少 30 次报告 median/p95；冷启动至少 5 次报告全部样本与中位数，不用少量样本伪装可靠 p95。
4. 先形成 baseline 再启用门槛：同机重复测量后关键路径回退超过 10% 必须解释或修复；安全修复的必要成本单独批准记录。虚拟 CI runner 不使用脆弱的绝对毫秒断言。
5. 目标参考：5,000 文档热搜索 p95 ≤200 ms，普通本地文档首屏 ≤1 s，开关 20 次后无单调资源增长。均是候选目标，不是已经达到的性能；若基线证明不合理，记录校准依据。
6. 新增开发脚本后才在 CI/说明中调用；不要引用不存在的 `--ignored` 压测命令。[R01]

**验收**：脚本可重跑；原始样本和环境可审查；不存在只报“感觉更流畅”的结论；D09/D11/D12/D14 使用同一套指标。

### D11 — EPUB 章节/图片与代码高亮的按需开销

**修改位置**：`EpubReader.tsx`、`MarkdownRenderer.tsx`、图片/高亮辅助模块；依赖多格式稳定性任务。

**实现顺序**：

1. 图片先修 IPC 的实际懒加载：进入视口附近才调用资产读取；`<img loading="lazy">` 不能替代前面的按需读取。[R11]
2. 同文档/同版本/同 assetId 合并进行中请求，限制并发；错误重试独立；Blob URL 共享必须有引用计数，在最后消费者释放后撤销。
3. EPUB 优先优化章节几何测量：使用可见/邻近集合，不在每帧测量整书；章节渲染先尝试无侵入延迟策略。不要直接套普通列表虚拟化破坏跨段选区。
4. 若采用章节窗口化，先实现“搜索/目录/标注跳转→目标章 materialize→布局稳定→定位”；保留跨章选区、查找和位置恢复的既有语义。无法保证时保留 DOM，选择较保守方案并用数据说明。
5. Shiki 增加以代码+语言+主题/高亮版本为键的有界缓存；只为真正需要的代码执行高亮；失败 Promise 从缓存移除，允许重新加载。[R13]
6. 建议大于 100 KiB 或 2,000 行的单块先纯文本显示并提示，再按需高亮；候选阈值在 D10 校准。Worker 只在测量证明必要且 CSP 可保持不变时引入。
7. 检查当前 bundle，修复静态/动态混合导入导致的无效拆包；用实际产物确认，不把历史警告当作当前必然存在的问题。

**测试**：首屏不读取全书图片；并发数有上限；失败可重试；对象 URL 恰好释放；跳到未渲染章可定位；长代码仍可复制；无高亮语言不报错；标注/查找/书签/脚注/双栏无回退。

**验收**：提供具体前后数据；不以降低可用功能换性能；没有通过删除安全校验缩短耗时。

### D12 — 按业务边界拆分 App 与状态

**修改位置**：`src/App.tsx`、`src/store/useReaderStore.ts`、`src/components/`、`src/lib/`、相关 tests。

**目的**：降低修改耦合，不以文件行数充当质量指标。

**推荐提取顺序**：

1. 设置面板、欢迎页、最近书库菜单、通知组件等相对独立 UI。
2. 阅读会话/位置恢复、全局快捷键与浮层、标注操作、分栏、统计跟踪等 hooks。
3. 主页、统计、回顾、全库摘录的视图容器；保留已有 lazy 加载边界。
4. store 按书库、导航、偏好等职责提取 slices/helper；只有实际需要跨视图共享的状态进入 store。

**约束**：

- 一次提取一个边界，保持函数行为、hook 顺序与生命周期；完成定向测试后再继续。
- 前置并发修复未通过时，不同时搬移对应逻辑。
- 新增模块遵循仓库 `src/lib/`、`components/` 习惯，不创建过度设计的通用插件/DI 框架。
- 不为了传参方便让子组件订阅整个 store；按必要 selector 订阅。
- 不把 `App` 拆成多个同样巨大的“manager”；不要增加 React effect 之间的隐式依赖链。

**验收**：App 主要负责布局与组合；关键业务可独立测试；相同场景无额外订阅/监听器泄漏、无首包明显膨胀；差异主要是可追踪的提取而非重写。

### D13 — Rust 模块与跨语言契约治理

**修改位置**：`src-tauri/src/library.rs`、`user_store.rs`、`lib.rs`、`src/lib/backend.ts`、`tauriBackend.ts` 与契约测试。

**实现**：

1. 先提取低风险纯函数/验证与测试，再拆存储实现；推荐领域：paths、library sessions、scan/index/cache、EPUB assets、annotations、collections、review、migrations。
2. `library.rs/user_store.rs` 可短期保留 re-export，减少一次迁移所有导入的风险；完整 command 注册清单仍能机械对账。
3. migrations 按版本保留原顺序和语义，不为消除重复改写已发布迁移步骤；新增修复用新 migration。
4. 对关键 DTO 增加双向 fixture：Rust serialize→TS 接收校验；TS 样本→Rust deserialize。覆盖 enum casing、optional/null、Unicode 长度、毫秒时间与整数范围。
5. 类型生成不是强制引入某个库。先比较现有契约测试与新增生成器成本；可选择最小生成工具，但不得为节省接口代码扩大运行时依赖。重复常量若必须双份，写精确的对账测试。
6. 只对本轮涉及的错误/会话 DTO 建立统一源，逐步迁移。避免一口气改变所有 IPC wire format 或旧导出格式。
7. 沿用现有命令清单对账测试，升级成能发现漏注册、漏 wrapper、拼写/参数变化的检查。[R01]

**验收**：旧 schema fixtures 与导入导出兼容；迁移测试不减少；锁顺序有文档与测试；commands 清单两侧一致；没有仅为模块拆分改变数据库数据。

### D14 — 样式分层、入口层级与可访问性

**修改位置**：`src/App.css`、`src/styles/`、D12 提取后的相关组件、样式/组件测试。

**实现**：

1. 按 tokens/base/layout/reader/components/format-specific 顺序拆样式；首轮保持导入顺序和选择器优先级，不同时换设计语言。
2. 为单功能确定样式归属，限制跨模块选择器；不为了拆文件引入 `!important` 覆盖链。
3. 设置入口仅做渐进整理：基础阅读设置优先，高级/实验功能收纳且有说明；已有功能必须仍可发现，命令面板与快捷键保留。不要随意改名或删除入口。
4. 优先补 dialog 焦点限制与关闭后焦点恢复、键盘导航、按钮可访问名称、状态播报、Esc 与全局快捷键冲突。
5. 真机/浏览器检查 1100×620、760×520、1280×720；明暗主题、三档动效、125%/150% 显示缩放、200% 文本缩放。截图基线使用自己的合成内容。
6. 竖排、PDF 原版式和分栏的能力差异保持既有明确提示；不要为了统一设置强行开启不兼容功能。

**验收**：拆样式前后视觉对比无意外回退；窄窗不遮挡关键操作；全键盘能完成开库/切文档/标注/关闭浮层；用户原有排版偏好不被重置。

### D15 — 本地诊断、备份与启动失败恢复

**修改位置**：桌面设置/诊断组件、Rust 新的受限诊断与恢复模块、`lib.rs`、facade、`docs/USER_GUIDE.md`。

**最小产品范围**：一个桌面“本地数据与诊断”入口，不新增主页仪表盘。

**实现**：

1. 展示实际应用版本、数据库健康/迁移状态、用户数据与缓存位置、缓存占用、失败索引数、最近备份时间、未保存会话数量。
2. 操作仅含：重试失败项、清理派生缓存、导出脱敏诊断、创建本地备份、从备份恢复；危险操作有预览与确认。
3. 备份不是把“摘录导出”改个名字：先盘点现有 archive V2 是否覆盖感悟、回顾、合集、统计、偏好与位置。缺失项明确列出，不静默宣称全量。[R07][R14]
4. 完整备份保留用户库、统计库及轻量偏好/位置快照，manifest 带格式版本、应用版本、文件校验和与包含范围。默认不打包原书库正文，也不备份可再生转换缓存。
5. 跨数据库一致性要有策略：短暂暂停本应用写入、排空受控队列、逐库快照，然后恢复；不能把两份任意时间点的复制称为原子全应用快照。多实例必须互斥或拒绝操作。
6. 恢复在 staging 中校验来源、格式、schema、文件白名单、摘要和数据库完整性；恢复前先备份当前数据，不自动覆盖原文档库。提供中断后恢复和回滚标记。
7. 启动初始化失败不得只 panic：提供最小可运行的恢复界面/系统错误入口。数据库不可用时正常读写命令明确拒绝；诊断/选择备份入口不依赖健康书库已打开。
8. 不启用云上传；日志默认脱敏。数据库位置只在本机明确展示，不进入自动导出日志。
9. 无真实数据授权时只实现和测试恢复能力，不替用户恢复当前数据库。

**测试**：损坏用户库仍可进入恢复入口；损坏统计库不删除用户库；磁盘满备份失败不覆盖旧备份；路径逃逸/多余文件被拒绝；有 WAL 的备份可恢复；旧备份导入后数据核对；完整恢复保留用户偏好与统计；未签名/不匹配格式只作本地不可信输入校验，不假装有密码学真实性。

**验收**：一份合成完整数据可在空白测试 profile 恢复，关键业务记录逐项一致；没有空库替换损坏库的“自动修复”；备份范围与限制可见。

### D16 — 最终回归、安装包与交接

**执行**：

1. 运行第 5 节完整命令与已新增脚本；保留环境、退出码、摘要及失败日志。
2. 在真实 Windows Tauri/WebView2 完成第 6 节关键链路，浏览器共享 UI 测试独立标识。
3. 用合成旧数据测试升级/迁移，用新安装测试首次启动；确认标注、统计和偏好并未因安装路径变化丢失。
4. 生成本地 NSIS 安装包并记录 SHA-256、版本和验证平台；不要自动上传/发布。签名未配置就明确标为未签名，不声称已完成代码签名。
5. 检查 `git diff --check`、改动范围、生成文件和敏感内容；保留用户原有改动。
6. 对照 30 项映射逐项结项；可选 Web 未做应标 `DEFERRED`，不是失败，也不是已修复。
7. `FINAL_REPORT.md` 输出：修复的可复现问题、实施的优化、实测前后指标、数据兼容性、完整验证、未验证项、剩余风险与撤回说明。
8. 更新用户文档及必要架构说明；若 `AGENTS.md` 中事实已过时，生成最小建议 diff，未经明确授权不自动改写约定。

**验收**：没有隐藏失败；实现与文档相符；未完成真机、恢复或安装测试时不得写“全量验收通过”。

## 5. 验证命令与运行规则

以下命令来自当前仓库已有脚本/工具；在仓库根目录执行。先跑定向测试，再跑完整验证。[R01][R15]

```powershell
# 基线与工具链
git status --short
git rev-parse HEAD
node --version
pnpm --version
rustc --version
cargo --version

# 按锁文件安装；若失败，先区分锁文件不一致与网络/环境问题
pnpm install --frozen-lockfile

# 全量前端验证
pnpm test
pnpm exec tsc --noEmit
pnpm build

# Rust 验证
cargo test --locked --manifest-path src-tauri/Cargo.toml
cargo fmt --manifest-path src-tauri/Cargo.toml --check
cargo clippy --locked --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings

# 冻结 Web 的构建兼容检查，不表示 Web 全功能验收
pnpm build:web

# 最终桌面打包；会重新构建桌面前端，避免把 Web dist 当桌面包
pnpm tauri build

git diff --check
```

定向前端用例示例：

```powershell
pnpm exec vitest run src/lib/useDocumentAnnotations.test.ts
pnpm exec vitest run src/lib/readingTracker.test.ts
pnpm exec vitest run src/components/PdfReader.test.tsx src/components/EpubReader.test.tsx
```

**执行前确认实际测试文件名**，文件不存在时定位现有测试或新增对应文件，而不是制造“命令通过但没有测试被选中”的结果。Rust 定向测试使用真实已存在的测试名。

Windows 命令脚本须在非零 `$LASTEXITCODE` 时失败退出；不要把前一命令失败被后一命令成功覆盖。新增脚本必须记录命令契约、平台要求和测试数据清理范围。

环境限制的处理：没有 Windows/WebView2 时可以完成纯逻辑与源码工作，但 Tauri IPC、安装包运行、系统文件对话框和真机性能必须列为 `NOT_RUN`。不得修改产品逻辑掩盖 SDK、证书或网络安装问题。

## 6. 必须保留的验收矩阵

| 编号 | 场景 | 预期证据 |
|---|---|---|
| A01 | 同路径 MD/PDF/EPUB 跨 A/B 库切换 | 内容、状态、资产、错误与写入归属均正确 |
| A02 | A→B→A，旧读取/索引事件迟到 | 首次 A 的结果不提交 |
| A03 | 普通刷新与文件改动 | 普通刷新不打断阅读；改动使相应源版本失效 |
| A04 | 标注保存/清空/撤销过程中切文档 | 新文档不被旧 mutation 修改 |
| A05 | reload 与 note/color 并发 | 已成功写入字段不倒退、不被覆盖 |
| A06 | 标注迁移，有 WAL、冲突、只读/磁盘满 | 数据校验一致或安全失败，源不损坏 |
| A07 | 清理缓存 | 用户库、统计、偏好与备份不受影响 |
| A08 | 阅读统计切库、重试、重复/乱序保存 | 正确库、幂等、不倒退、失败可见 |
| A09 | PDF Range 错误、超时、取消和重试 | 能退出加载态；无假成功或悬挂任务 |
| A10 | PDF 原版式/阅读模式/缩放/双页/区域引用 | 原有交互可用，位置与标注不漂移 |
| A11 | 双 EPUB、EPUB+MD/PDF、同书双开 | 资源与跳转不互相破坏 |
| A12 | EPUB 资源超额、非法内容、正常图文书 | 有界拒绝异常；正常样本通过 |
| A13 | 长 EPUB 未渲染章节的搜索/标注/目录跳转 | 能 materialize 后定位，选区语义保留 |
| A14 | 20 次开关文档及双栏 | 句柄、订阅、Blob URL 无持续泄漏 |
| A15 | 大库搜索、扫描、代码高亮 | 同环境前后数据与预算报告 |
| A16 | 明暗主题、窄窗、不同缩放、无动效 | 截图与手工检查，不只 DOM 快照 |
| A17 | 键盘、dialog 焦点与关闭恢复 | 不陷焦点、不触发冲突快捷键 |
| A18 | 损坏数据库启动、备份与空 profile 恢复 | 有恢复入口、数据逐项对账 |
| A19 | 首次安装与旧版合成数据升级 | 正常启动，不丢设置/历史数据 |
| A20 | Web 构建与已有示例阅读 | 共享改动未破坏封存路径 |

每行记录执行平台、构建类型、结果 `PASS/FAIL/NOT_RUN` 与证据。自动化、浏览器、Tauri 真机属于不同证据层，不互相冒充。

## 7. 可选 Web 工作包（默认 DEFERRED）

这些工作包保留前次审查的覆盖面，但不自动解除 Web 功能冻结。[R01]

### W01 — Web 搜索与加载请求治理

**对应**：`src/lib/webLibrary.ts`。[R16]

- manifest/searchIndex 缓存进行中的 Promise，失败清除；clearCache 增加 generation，旧请求不得重新填回新缓存。
- request 支持取消/超时；所有消费者共享请求时不能由一个调用者随意取消他人请求。
- 预计算归一化文本，测量后再移入 Worker；只有达到实测规模门槛才分片/倒排，不为小示例库引入大型搜索服务。
- 验收：并发加载只发一次；失败可重试；刷新期间旧结果不回填；worker 与现有搜索结果语义一致；相对 base 路径不回退。

### W02 — Service Worker 应用壳、scope 与缓存容量

**对应**：`public/sw.js`、注册入口、Vite 构建插件。[R17]

- 从构建产物产生应用壳 precache 清单，首装结束前确保入口 HTML/JS/CSS 依赖自洽；不要只缓存 HTML。
- 缓存名加入规范化 scope + buildId；只能删除本 scope 的旧缓存，不影响同源其他项目。
- 应用壳按完整版本集合管理，避免为满足条目数随机逐出当前版本必须资源；旧版本整体淘汰。
- 内容缓存按明确 FIFO 或真正 LRU 实现，不把插入顺序称为访问热度；增加总字节预算和配额异常处理。
- 更新过程防新 HTML 配旧/缺失 JS；离线、缓存缺失和网络 5xx 的行为均明确。
- 验收：首次安装→离线重载；两 scope 共存；升级时断网；旧页面保持打开；配额不足；清理不跨 scope。

### W03 — 文档、manifest 与搜索的发布版本一致性

**对应**：生成器、`webLibrary.ts`、service worker。[R16][R17][R18]

- manifest/search/正文引用带统一构建身份，已打开客户端固定一代；发现更新时明确刷新或安全切换。
- 允许短期保留完整旧代，但不能把旧检索与新正文无声混搭。
- 验收：构建 V1→V2，分别只更新 manifest/search/正文及断网场景，客户端保持一致或明确报更新失败。

### W04 — 静态发布安全与输出替换

**对应**：`scripts/generate-web-library.mjs` 及现有 tests。[R18]

- 增加公开清单/显式排除和 dry-run 报告，列出将发布的相对路径与数量；仅扩展名符合不等于内容可公开。
- 校验规范化输入/输出路径、父级符号链接与危险目标；输出目录需有生成器归属标记。首次处理无标记现有目录时拒绝删除，不猜它是生成目录。
- staging→保留旧输出→发布新输出→成功后清理；Windows 无法保证目录原子替换的情况使用可恢复 journal，不笼统宣称全平台原子。
- 失败回滚保留上一版；不自动删除源目录/未知目录，不放宽安全资源格式范围。
- 验收：路径嵌套、同路径、符号链接、未知输出目录、中断/重命名失败、私密 Markdown 排除、相对链接与现有示例站构建。

## 8. 前次 30 项审查意见的覆盖映射

| 原编号 | 审查项 | 本计划任务 |
|---|---|---|
| 1 | 标注数据库迁出缓存语义目录 | D04 |
| 2 | 标注写操作上下文保护 | D02、D03 |
| 3 | Rust 跨库竞态 | D02 |
| 4 | PDF Range 错误处理 | D06 |
| 5 | EPUB 单书缓存与分栏 | D07 |
| 6 | EPUB DOM 锚点隔离 | D07 |
| 7 | 统计绑定原书库 | D05 |
| 8 | 已结束会话的失败重试 | D05 |
| 9 | EPUB 解压/解析预算 | D08 |
| 10 | 全局锁与阻塞扫描 | D09 |
| 11 | EPUB 图片实际按需读取 | D07、D11 |
| 12 | EPUB 整书渲染与滚动测量 | D10、D11 |
| 13 | 代码高亮预算与重试 | D11 |
| 14 | App 职责拆分 | D12 |
| 15 | Rust 大文件拆分 | D13 |
| 16 | Rust/TS 契约重复 | D02、D13 |
| 17 | 全局样式拆分 | D14 |
| 18 | 错误模型与无声失败 | §3.2、D06、D15 |
| 19 | 自动测试门禁 | D01 |
| 20 | 组合场景回归 | D00–D16、验收矩阵 |
| 21 | 恢复演练与启动恢复模式 | D04、D15 |
| 22 | 工具链与依赖审计 | D01 |
| 23 | Web 主线程搜索 | W01，默认延后 |
| 24 | Web 请求合并与缓存世代 | W01，默认延后 |
| 25 | PWA 首装离线启动 | W02，默认延后 |
| 26 | SW scope/容量/淘汰 | W02，默认延后 |
| 27 | Web 发布版本一致性 | W03，默认延后 |
| 28 | 静态发布安全/替换 | W04，默认延后 |
| 29 | 功能层级与高级入口 | D14，保持小范围 |
| 30 | 书库健康/本地诊断 | D15 |

## 9. 完成定义与停止规则

**可以宣告默认计划完成的条件**：

- D00–D16 均有明确结项；没有把环境限制写成通过。
- 确认的关键数据与跨库缺陷均有失败前/通过后回归证据。
- 不改写用户文档库，现有用户数据与导入导出兼容。
- 性能优化有实际前后结果；纯重构没有夹带行为变化。
- Windows 真机、备份恢复、安装包验证均完成，或者最终交付明确标为尚待验收，不能称为完全验收。
- Web 保持现状兼容；可选 W 任务仍延后时不计入默认完成承诺。

**必须停止相关写操作、但可以继续独立任务的情况**：真实新旧数据库均有数据且无法确认权威版本；迁移校验失败；需要扩大文件权限/CSP；需要覆盖用户未提交修改；需要发布、提交、推送或远端管理权限。

**检查点必须包含**：当前 HEAD 和原有改动摘要；刚完成任务；正在修改文件；最后运行的命令和退出码；未完成测试；下一条可直接执行的动作。不要只留下“继续优化”或“下一步测试”。

## 10. 证据来源与适用性

仓库证据固定于上述审查提交；Agent 实施时须重新核对实际 HEAD。文件链接用于定位代码事实，本文的候选阈值、模块设计和阶段安排是实施建议，不是仓库已实现功能。

[R01]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/AGENTS.md "工作约定、Web 封存、命令与高风险区域"
[R02]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/src/store/useReaderStore.ts "前端书库/文档/搜索请求世代"
[R03]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/src-tauri/src/library.rs "open_library、open_document、read_pdf_reading_mode、Range 与 EPUB 资产"
[R04]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/.github/workflows/deploy-pages.yml "现有 Pages 发布工作流"
[R05]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/src/lib/useDocumentAnnotations.ts "reload 与 mutation/撤销状态回写"
[R06]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/src-tauri/src/lib.rs "启动顺序、数据库路径与 command 注册"
[R07]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/src-tauri/src/user_store.rs "用户数据迁移、备份、旧数据救援及业务存储"
[R08]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/src-tauri/src/stats.rs "统计会话归属与 upsert"
[R09]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/src/lib/readingTracker.ts "周期保存、结束会话与失败处理"
[R10]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/src/components/PdfReader.tsx "ReadeRangeTransport 与 PDF 生命周期"
[R11]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/src/components/EpubReader.tsx "图片加载、DOM 锚点与章节渲染"
[R12]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/src-tauri/src/documents.rs "inspect_epub_container 与安全 DTO"
[R13]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/src/components/MarkdownRenderer.tsx "Shiki 按需加载与高亮"
[R14]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/src/App.tsx "现有 archive V2 使用、设置与全局编排"
[R15]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/package.json "当前脚本与工具依赖"
[R16]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/src/lib/webLibrary.ts "Web 搜索、manifest/index 加载与缓存"
[R17]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/public/sw.js "应用壳和内容缓存策略"
[R18]: https://github.com/RsLuna7/reade/blob/94ef1dd94c0576f5916ac5935458876d4feca249/scripts/generate-web-library.mjs "公开文件筛选与输出目录替换"
[O01]: https://docs.rs/tauri/latest/tauri/path/struct.PathResolver.html "Tauri 各平台 data/cache/local_data 路径；实施时核对锁定版本"
[O02]: https://v2.tauri.app/develop/calling-rust/ "Tauri command、异步调用与错误处理"
[O03]: https://www.sqlite.org/backup.html "SQLite 一致性在线备份"
[O04]: https://www.sqlite.org/lang_vacuum.html "VACUUM INTO 行为和限制"
