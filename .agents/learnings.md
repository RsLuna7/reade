# Agent learnings (staging)

原始纠错暂存。**不进 always-on 上下文。** 蒸馏请手动调用 `/improve-reade-agents`。

## 怎么追加

每条四行：日期、任务、Agent 做错了什么、正确做法与 Why。能泛化则标 `promote`；一次性口味标 `one-off`。同类失误能用测试抓住时，写「先补测试」而不是原则。

## Entries

（暂无。）
