---
name: vercel-composition-patterns
description: >
  React composition patterns that scale. Use when refactoring Reade components
  with boolean prop proliferation, designing reusable component APIs, or working
  with compound components, render props, context providers, or React 19 APIs
  (ref as a prop, use() instead of useContext). Do not load for Rust, IPC,
  CSS-only, or unrelated bugfixes.
---

# React composition patterns (on demand)

This skill is **not** always-on. Load it only when the task matches the description.

1. Read `tools/skills/vercel-composition-patterns/SKILL.md` for the index.
2. Read only the matching files under `tools/skills/vercel-composition-patterns/rules/`.
3. Read `tools/skills/vercel-composition-patterns/references/full-guide.md` only if a rule file is not enough.

Do not copy this guide into `AGENTS.md`. Do not create a nested `AGENTS.md` inside `tools/skills/` (Cursor would inject it on every task).
